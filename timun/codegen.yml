overwrite: true
schema: "http://localhost:9090/graphql/"
documents: "src/**/*.graphql"
generates:
  src/types/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"

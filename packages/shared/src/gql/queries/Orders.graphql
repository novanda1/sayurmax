query Orders(
    $status: OrderStatusCode!
    $limit: Int!
    $after: String
    $date: OrderDate
) {
    orders(status: $status, limit: $limit, after: $after, date: $date) {
        ...OrdersResponse
    }
}

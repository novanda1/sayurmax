mutation Register($options: CreateUserDto!) {
    register(options: $options) {
        error {
            ...ErrorFragment
        }
        user {
            ...UserFragment
        }
        token
    }
}
